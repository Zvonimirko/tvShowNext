import css from "styled-jsx/css";

const thumbnailStyles = css`
  .thumbnail__image {
    width: 100%;
    height: 500px;
    object-fit: contain;
  }

  .thumbnail__caption {
    text-align: center;
    padding: 10px;
  }
`;

export default thumbnailStyles;
